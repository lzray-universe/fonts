# fonts.lzray.com — 主站（静态）+ 反代 API 与 CSS
fonts.lzray.com {
    encode gzip zstd

    root * /var/www/fonts-site
    file_server

    # 主站缓存适中
    header {
        Cache-Control "public, max-age=600"
    }

    @api path /api* /css2*
    handle @api {
        reverse_proxy 127.0.0.1:8787
    }
}
